CONFIG_ZMK_USB=y
CONFIG_LOG_PROCESS_THREAD_STARTUP_DELAY_MS = 8000

if SHIELD_CROSSES_RIGHT

config ZMK_KEYBOARD_NAME
  default "Crossyboi BLE"

config ZMK_SPLIT_ROLE_CENTRAL
  default y

endif

if SHIELD_CROSSES_LEFT || SHIELD_CROSSES_RIGHT

config ZMK_SPLIT
  default y

endif
